# Weather Forecast App

A weather forecast web app built with HTML, CSS and JavaScript.

## Features
- Search weather by city name
- Shows current temperature and conditions
- Fetches live data from a weather API

## Setup
1. Get a free API key from your weather API provider
2. Replace `YOUR_API_KEY` in `script.js` with your key
3. Open `index.html` in your browser

